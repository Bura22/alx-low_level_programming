#include "main.h"

/**
 * mul - compute multiplication of two numbers
 * @a: The second number to be calculated
 * @b: The second number to be calculated
 * Return: Always mult
 */

int mul(int a, int b)
{
	int mult;

	mult = a * b;

	return (mult);
}
